# Sakura AI 测试用例生成提示词完整文档

> 本文档整理了Sakura AI系统中所有AI生成相关的提示词，包括需求文档生成、测试场景拆分、测试点生成、测试用例生成的完整流程。

**文档版本**: v1.0  
**更新日期**: 2026-03-02  
**维护团队**: Sakura AI开发团队

---

## 目录

1. [概述](#概述)
2. [需求文档生成提示词](#需求文档生成提示词)
   - 2.1 [Web端需求文档生成](#21-web端需求文档生成)
   - 2.2 [移动端需求文档生成](#22-移动端需求文档生成)
   - 2.3 [文本文档需求提取](#23-文本文档需求提取)
   - 2.4 [通用指令和格式规范](#24-通用指令和格式规范)
3. [测试场景生成提示词（阶段1）](#测试场景生成提示词阶段1)
4. [测试点生成提示词（阶段2）](#测试点生成提示词阶段2)
5. [测试用例生成提示词（阶段3）](#测试用例生成提示词阶段3)
6. [项目配置集成](#项目配置集成)
7. [RAG知识库增强](#rag知识库增强)
8. [修改页面模式](#修改页面模式)
9. [提示词调用流程](#提示词调用流程)
10. [附录：关键代码位置](#附录关键代码位置)

---

## 概述

Sakura AI采用多阶段AI生成策略，将测试用例生成分为4个阶段：

1. **需求文档生成**：从Axure原型HTML或文本文档提取结构化需求
2. **测试场景拆分（阶段1）**：将需求文档拆分为3-6个测试场景
3. **测试点生成（阶段2）**：为每个场景生成3-10个测试点
4. **测试用例生成（阶段3）**：为每个测试点生成1个测试用例

每个阶段都有专门的提示词模板，确保AI生成的质量和一致性。

---

## 需求文档生成提示词

### 2.1 Web端需求文档生成

**文件位置**: `server/prompts/web-requirement.prompt.ts`

**核心功能**:
- 从Axure Web原型HTML生成结构化需求文档
- 识别页面类型（列表页/表单页/详情页/弹窗）
- 提取查询条件、列表字段、表单字段、操作按钮
- 提取业务规则（特别是红色文字标注的核心规则）

#### 页面类型识别规则

**识别优先级**（从高到低）：

1. **第一优先级：看按钮文本**（最重要！）
   - **表单页按钮特征**：
     - 文本包含"保存"/"提交"/"撤销"/"取消"/"确认提交"/"创建"/"新建"/"确定" → **表单页**
     - ⚠️ "撤销"按钮是表单页的典型标志，详情页不会有撤销按钮
   - **列表页按钮特征**：
     - 文本包含"查询"/"搜索"/"导出"/"批量删除"且有表格结构 → **列表页**
   - **详情页按钮特征**：
     - 只有"编辑"/"返回"/"打印"/"关闭"且无可编辑控件 → **详情页**

2. **第二优先级：看可编辑控件**
   - 有以下任一类型即为表单页：
     - `<input type="text/number/password/email">` 输入框
     - `<textarea>` 多行文本框
     - `<select>` 下拉选择框
     - 单选按钮（`<input type="radio">`）→ 重要！单选按钮一定是表单页
     - 多选框（`<input type="checkbox">`）
   - 完全没有可编辑控件 → **详情页**

3. **第三优先级：看页面结构**
   - 有 `<table>` 或 `class="table"` 且有"查询"按钮 → **列表页**
   - 页面较小且有"确定"/"取消"按钮 → **弹窗**


#### 查询条件识别规则

**核心规则**：查询条件 ≠ 表单字段

**识别步骤**：
1. 先找到"搜索"或"查询"按钮（文本包含"搜索"/"查询"/"重置"的button或div）
2. 向上遍历DOM树，找到该按钮所在的父容器（通常是form或div）
3. 提取该容器内的所有输入控件（input、select、date-picker等）
4. 这些控件就是"查询条件"，不要归类为"表单字段"

**示例HTML结构**：
```html
<div class="query-section">
  <input placeholder="订单号" />
  <select><option>全部状态</option></select>
  <button>搜索</button>
  <button>重置</button>
</div>
```

**说明**：上述input和select应识别为**查询条件**，不是表单字段

#### 列表字段识别规则

- 找到表格的列头行（通常是 `<thead>` 或第一个 `<tr>`）
- 从 `<th>` 或 `<div class="label">` 中提取字段名
- 如果有"操作"列，提取该列中的所有按钮（详情、编辑、删除等）

**Axure表格结构示例**：
```html
<div class="table">
  <div class="row header">
    <div class="cell"><div class="label">订单号</div></div>
    <div class="cell"><div class="label">客户名称</div></div>
    <div class="cell"><div class="label">操作</div></div>
  </div>
</div>
```

#### 操作按钮去重规则

**问题**：列表页中的"详情"按钮可能在每一行都出现，导致重复识别

**去重策略**：
1. 提取所有按钮的文本内容（button、a标签、可点击的div）
2. 如果同一按钮文案出现多次（如"详情"出现10次）：
   - 只在"操作按钮"表格中记录**一条**
   - 在"位置"列标注"每行操作列"或"表格操作列"
3. 如果按钮只出现一次（如页面顶部的"新增"按钮）：
   - 正常记录，标注具体位置（如"页面顶部"）


#### 弹窗/对话框识别规则

**Axure弹窗特征识别**（优先级从高到低）：

1. **通过data-label属性识别**（最准确）：
   - 查找 `data-label` 属性包含"弹窗"、"对话框"、"确认"、"提示"、"审核"、"下单"等关键词的元素
   - 示例：`<div data-label="审核弹窗">`、`<div data-label="下单确认弹窗">`
   - **必须使用data-label的值作为弹窗名称**

2. **通过class名识别**：
   - class包含："modal"、"dialog"、"popup"、"layer"、"panel_state"
   - Axure特有：`class="ax_default_hidden"` 表示默认隐藏的弹窗

3. **通过结构特征识别**：
   - 包含"确定"和"取消"按钮的小型容器（非整个页面）
   - 包含"同意"/"拒绝"、"是"/"否"等对立选项的容器
   - 有单选框或多选框组合表单的独立容器

4. **通过style属性识别**：
   - `display:none` 或 `visibility: hidden` 表示初始隐藏
   - `position: fixed` 或 `position: absolute` 表示浮层

**错误弹窗过滤规则**：
- 忽略导航相关：用户信息下拉框（如"修改密码"/"退出"菜单）、侧边栏菜单
- 忽略下拉选项：下拉菜单的选项列表、更多操作的下拉面板
- 只识别业务操作弹窗：与数据提交、审核、确认等业务操作相关的模态弹窗

#### 业务规则提取规则

**从Axure注释表格中提取业务规则**（关键！）

**识别场景**：
1. 独立的业务规则说明表格（class="table"且包含"业务规则"列）
2. 字段说明表格中的"备注"/"需求描述"列
3. 按钮/功能说明表格中的"备注"/"需求描述"/"补充说明"列
4. **红色文字标注的重要规则**（`style="color:#D9001B"` 或 `style="color:red"`）

**提取规则**：

1. **查找业务规则表格结构**：
```html
<div class="table">
  <div class="row header">
    <div class="cell">按钮/图标/模块</div>
    <div class="cell">备注</div>
  </div>
  <div class="row">
    <div class="cell">列表数据</div>
    <div class="cell">
      1、结算总金额计算规则...
      2、审核规则...
      3、拦截规则...
    </div>
  </div>
</div>
```

2. **识别关键特征**：
   - 表头包含："备注"、"需求描述"、"业务规则"、"注意事项"、"补充说明"
   - 单元格内容特征：包含数字编号、红色文字标注、长段落文字说明
   - 内容关键词："需要"、"必须"、"禁止"、"校验"、"拦截"、"提示"、"计算"

3. **红色文字识别**（极重要！）：
```html
<span style="color:#D9001B;">审核通过时,需要校验库存...</span>
<span style="color:red;">必须拦截并提示...</span>
```

**说明**：凡是红色文字，必须作为核心业务规则提取，可以在规则前加 ⚠️ 标记


#### 输出格式规范

**章节编号规则**（极其重要！必须严格遵守！）：

```markdown
# 系统名称 - 模块名称需求文档

## 1. 功能模块

### 1.1 订单列表页面                    ← 主章节，编号 1.1

#### 1.1.1 查询条件                     ← 子章节，编号 1.1.1（不是 1.1）
| 字段名 | 控件类型 | 必填 | 默认值 | 说明 | 来源 |
|--------|---------|------|--------|------|------|
| 订单号 | 文本框 | 否 | - | 输入订单号进行搜索 | 查询区域input |

#### 1.1.2 列表展示字段                 ← 子章节，编号 1.1.2（不是 1.1）
| 字段名 | 数据类型 | 格式 | 说明 | 来源 |
|--------|---------|------|------|------|
| 订单号 | 文本 | - | 显示订单编号 | 表格列头 |

#### 1.1.3 操作按钮                     ← 子章节，编号 1.1.3（不是 1.1）
| 按钮名称 | 位置 | 按钮类型 | 触发条件 | 操作说明 | 来源 |
|---------|------|---------|---------|---------|------|
| 查询 | 查询区域 | 主要 | 无 | 根据查询条件筛选数据 | 查询按钮 |

#### 1.1.4 业务规则                     ← 子章节，编号 1.1.4（不是 1.1）
1. 结算总金额计算规则...
2. ⚠️ 审核通过时，需要校验库存...（红色文字规则）
```

**编号规则**：
- 主章节使用三位编号：### 1.1, ### 1.2, ### 1.3...
- 子章节使用四位编号：#### 1.1.1, #### 1.1.2, #### 1.1.3...
- 每个子章节必须有不同的编号（1.1.1, 1.1.2, 1.1.3...）
- **千万不要所有子章节都用相同的编号！**

**Markdown井号层级规则**：
- "###" (三个井号) → 只用于主章节（如 ### 1.1 登录页面）
- "####" (四个井号) → 只用于子章节（如 #### 1.1.1 表单字段）
- **同级子章节必须使用相同数量的井号！**

---

### 2.2 移动端需求文档生成

**文件位置**: `server/prompts/mobile-requirement.prompt.ts`

**核心功能**:
- 识别移动端多状态画面（同一位置的多个状态切换）
- 识别TabBar导航、卡片列表、长屏详情、底部弹出层
- 提取下拉刷新、上拉加载、手势操作
- 识别权限申请、网络状态检测


#### 移动端多状态画面识别（极其重要！）

**核心问题**：移动端Axure原型中，经常会在同一个位置出现多个画面，这些画面通过 `display:none` 切换，代表不同的状态。

**识别策略**：

1. **查找隐藏元素**：
   - 搜索包含 `style="display:none"` 或 `style="visibility:hidden"` 的div元素
   - 搜索包含 `class="ax_default_hidden"` 的元素

2. **判断是否为同位置多状态画面**：
   - 提取所有疑似状态画面的 `data-left` 和 `data-top` 属性值
   - 如果多个元素（包括可见和隐藏）的位置坐标完全相同或非常接近（相差<50px）
   - 则判定为**同一位置的多状态画面**

**示例结构**：
```html
<!-- 状态1: 正常有数据(可见) -->
<div id="u3556" data-left="0" data-top="100" style="display:block">
  <div class="text">您的购物车</div>
  <button>去结算</button>
</div>

<!-- 状态2: 管理模式(隐藏) -->
<div id="u3601" data-left="0" data-top="100" style="display:none">
  <div class="text">您的购物车</div>
  <button>删除</button>
  <button>退出管理</button>
</div>

<!-- 状态3: 空状态(隐藏) -->
<div id="u3650" data-left="0" data-top="100" style="display:none">
  <div class="text">您还没有添加任何商品哦~</div>
  <button>去逛逛</button>
</div>
```

3. **识别状态类型**：

**空状态（Empty State）**：
- 文本关键词："暂无数据"、"空空如也"、"还没有"、"快去"、"去逛逛"
- 按钮文案："去添加"、"去逛逛"、"立即体验"

**加载中状态（Loading State）**：
- 文本关键词："加载中..."、"正在加载"、"请稍候"
- 图标特征：class包含"loading"、"spinner"、"skeleton"

**正常/有数据状态（Normal State）**：
- 包含实际的数据列表、卡片、表格
- 按钮类型："详情"、"编辑"、"删除"、"去结算"

**编辑/管理状态（Edit State）**：
- 文本关键词："管理"、"编辑"、"完成"、"退出管理"、"批量删除"
- UI特征：出现删除按钮、复选框、批量操作按钮

**错误状态（Error State）**：
- 文本关键词："加载失败"、"网络异常"、"请求超时"、"出错了"
- 按钮文案："重试"、"刷新"、"返回"


#### 移动端输出格式

```markdown
### 1.1 购物车页面

#### 功能描述
用户可查看购物车商品，进行结算、管理（删除）等操作。

#### 页面类型
移动端列表页（多状态）

---

#### 🔄 状态切换说明
该页面存在以下3种状态，根据不同场景切换：
1. **初始状态（有商品）**：默认显示，购物车有商品时
2. **管理状态**：用户点击"管理"按钮后进入
3. **空状态**：购物车无商品时显示

---

#### 1.1.1 初始状态（有商品）
**触发条件**：购物车中有商品
**UI元素识别**：div#u3556, data-left="0", data-top="100", display:block

##### 列表展示字段
| 字段名 | 数据类型 | 格式 | 说明 |
|--------|---------|------|------|
| 商品图片 | 图片 | - | 商品主图 |
| 商品名称 | 文本 | - | 商品标题 |

##### 操作按钮
| 按钮名称 | 位置 | 操作说明 |
|---------|------|---------|
| 管理 | 页面右上角 | 进入管理状态 |
| 去结算 | 页面底部 | 跳转到结算页面 |

---

#### 1.1.2 管理状态
**触发条件**：用户点击"管理"按钮
**UI元素识别**：div#u3601, data-left="0", data-top="100", display:none

##### 操作按钮
| 按钮名称 | 位置 | 操作说明 |
|---------|------|---------|
| 退出管理 | 页面右上角 | 返回初始状态 |
| 删除 | 页面底部 | 删除选中的商品 |

---

#### 1.1.3 空状态
**触发条件**：购物车无商品
**UI元素识别**：div#u3650, data-left="0", data-top="100", display:none

##### 页面元素
- 空状态图标（购物车空箱子图标）
- 提示文字："您还没有添加任何商品哦~"

##### 操作按钮
| 按钮名称 | 位置 | 操作说明 |
|---------|------|---------|
| 去逛逛 | 页面中央 | 跳转到首页或商品列表页 |
```

#### 移动端特有组件识别

**下拉刷新（Pull to Refresh）**：
- 识别特征：页面顶部有"下拉刷新"提示文字或图标
- class包含："pull-refresh"、"refresh"、"pulldown"
- 需求文档说明："支持下拉刷新，刷新后重新加载最新数据"

**上拉加载更多（Load More）**：
- 识别特征：列表底部有"加载更多"、"上拉加载"文字
- class包含："load-more"、"infinite-scroll"、"pullup"
- 需求文档说明："支持上拉加载更多，每次加载20条数据"

**Tab选项卡切换**：
- 识别特征：页面顶部或中部有多个tab标签
- 文案："全部"、"待支付"、"待发货"、"已完成"
- class包含："tabs"、"tab"、"segment"

**手势操作**：
- 长按（Long Press）："长按删除"、"长按可编辑"
- 左滑/右滑（Swipe）："左滑删除"、"右滑返回"
- 双击（Double Tap）："双击放大"、"双击点赞"

---

### 2.3 文本文档需求提取

**文件位置**: `server/prompts/text-requirement.prompt.ts`

**核心功能**:
- 从PDF/DOCX/Markdown/TXT提取需求
- 不依赖DOM结构，从自然语言段落抽取
- 提取功能场景、数据字段、业务规则、流程状态

**文本解析原则**:
1. 输入是自然语言文档，而非HTML/DOM
2. 不要依赖标签、class、按钮或表格结构
3. 逐段落阅读，提取清晰的业务事实、流程、规则、字段
4. 只输出文档中出现的真实信息，禁止自造字段或示例

**必须提取的要点**:
- **功能/场景**：核心目标、前置条件、后置结果
- **数据/字段**：实体、字段含义、类型/格式/长度/必填、枚举值
- **业务规则**：计算规则、校验规则、审批/流转规则、拦截条件、时效要求
- **流程与状态**：步骤顺序、状态迁移、角色/权限要求
- **异常与边界**：失败/异常提示、重试/补偿、超时/并发/幂等
- **依赖**：外部系统/接口、数据来源/落地

**输出章节模板**:
```markdown
### 1. 功能概述
### 2. 业务流程
### 3. 数据与字段
| 字段名 | 数据类型/格式 | 必填 | 枚举/取值范围 | 说明 |
### 4. 业务规则
### 5. 校验与异常
### 6. 角色与权限
### 7. 接口/外部依赖
### 8. 其他要求
```

---

### 2.4 通用指令和格式规范

**文件位置**: `server/prompts/common-instructions.ts`

**核心功能**:
- 提供Markdown格式规范（标题层级、表格格式、列表格式）
- 提供质量检查清单（基础检查、内容完整性、来源标注）
- 提供常见错误及修正（10种常见错误示例）

#### Markdown格式规范

**标题层级规范**:
```markdown
# 一级标题: 文档标题（系统名称-模块名称需求文档）
## 二级标题: 功能模块（1. 功能模块1）
### 三级标题: 功能点（1.1 功能点名称）
#### 四级标题: 子章节（功能描述、页面类型、查询条件等）
##### 五级标题: 子功能点的子章节（如多状态画面的各状态）
```

**表格格式规范**:
- 必须使用Markdown表格语法
- 表头和数据行之间用 `|-----|` 分隔
- 每列用 `|` 分隔
- 表格前后要有空行

**列表格式规范**:
- 使用数字编号（1. 2. 3.）或（1、2、3、）
- 保持一致的编号风格
- 子列表使用缩进和 `-` 或 `*`

**特殊标记使用**:
- ⚠️ : 标记重要规则（从红色文字提取）
- 🔥 : 标记关键信息
- ✅ : 标记正确做法
- ❌ : 标记错误做法
- 🆕 : 标记新增功能
- ✏️ : 标记修改功能


#### 质量检查清单

**基础检查**:
- [ ] 文档标题是否包含系统名称和模块名称？
- [ ] 所有章节是否使用中文术语？（不要出现list、form、dialog等英文）
- [ ] 是否正确识别了页面类型？（列表页/表单页/详情页/弹窗）
- [ ] 表格格式是否正确？（有表头、分隔线、数据行）

**内容完整性检查**:
- [ ] 列表页是否包含"列表展示字段"章节？
- [ ] 是否提取了所有下拉框的选项值？
- [ ] 是否区分了"查询条件"和"表单字段"？
- [ ] 操作按钮是否去重？（如"详情"按钮在每行都有，只记录一条）
- [ ] 是否提取了Axure注释表格中的业务规则？
- [ ] 是否识别了所有红色文字规则并加⚠️标记？

**来源标注检查**:
- [ ] 所有字段是否标注了来源？（如"查询区域input"、"表格列头"）
- [ ] 弹窗是否标注了来源？（如"来源: data-label='审核弹窗'"）

#### 常见错误及修正

**错误1：页面类型识别错误**
- ❌ 错误：将有"保存"、"撤销"按钮的表单页识别为详情页
- ✅ 正确：按钮优先级最高，"保存"/"撤销"按钮 → 表单页

**错误2：查询条件和表单字段混淆**
- ❌ 错误：将查询区域的输入框归类为表单字段
- ✅ 正确："搜索"/"查询"按钮附近的输入框 → 查询条件

**错误3：列表展示字段缺失**
- ❌ 错误：列表页只提取了查询条件，没有列表展示字段
- ✅ 正确：列表页必须有"列表展示字段"章节，从表格列头提取

**错误4：按钮重复记录**
- ❌ 错误："详情"按钮出现10次，记录了10条
- ✅ 正确：只记录1条，位置标注"表格操作列（每行）"

**错误5：下拉框选项遗漏**
- ❌ 错误：只记录"订单状态"字段，没有提取选项
- ✅ 正确：可选值/枚举列填写"待审核/已审核/已完成/已取消"

**错误6：业务规则提取不完整**
- ❌ 错误：忽略了Axure注释表格中的业务规则
- ✅ 正确：必须从"备注"、"需求描述"列提取所有规则

**错误7：红色文字规则遗漏**
- ❌ 错误：没有识别原型中的红色文字
- ✅ 正确：查找 style="color:#D9001B" 或 style="color:red"，作为核心规则提取

**错误8：术语使用英文**
- ❌ 错误：页面类型写"form page"、控件类型写"input"
- ✅ 正确：页面类型写"表单页"、控件类型写"文本框"

**错误9：弹窗识别错误（移动端）**
- ❌ 错误：将用户信息下拉框识别为弹窗
- ✅ 正确：只识别业务操作弹窗，忽略导航下拉框

**错误10：多状态画面遗漏（移动端）**
- ❌ 错误：只识别了可见画面，忽略了隐藏状态（display:none）
- ✅ 正确：必须识别所有隐藏元素，通过坐标判断同位置多状态画面

---

## 测试场景生成提示词（阶段1）

**文件位置**: `server/services/functionalTestCaseAIService.ts` - `analyzeTestScenarios`方法

**核心功能**:
- 分析需求文档，拆分为不同的测试场景
- 识别页面类型（列表页/表单页/详情页/弹窗）
- 为每个场景关联需求章节、预估测试点数量
- 按优先级排序（high → medium → low）

### 测试场景拆分原则

**测试场景是什么？**
- 测试场景是一个具体的业务场景或功能场景
- 每个场景包含多个相关的测试点
- 场景应该聚焦于一个明确的测试目标

**根据页面类型识别测试场景**：

#### 列表页常见测试场景
1. **查询条件测试场景** - 验证查询功能、边界值、组合查询
2. **列表展示与数据校验场景** - 验证列表字段、数据格式、排序分页
3. **操作按钮与权限场景** - 验证新增、编辑、删除等操作功能和权限
4. **页面布局与文案校验场景** - 验证页面标题、字段标签、按钮文案、提示信息

#### 表单页常见测试场景
1. **表单字段验证场景** - 验证必填项、数据类型、长度限制
2. **字段联动与依赖场景** - 验证字段之间的联动关系
3. **提交与保存场景** - 验证提交、保存、取消等操作
4. **数据回显与编辑场景** - 验证编辑时数据回显
5. **页面布局与文案校验场景** - 验证页面标题、字段标签、按钮文案

#### 详情页常见测试场景
1. **详情展示场景** - 验证所有字段正确显示
2. **操作按钮场景** - 验证编辑、返回等操作按钮
3. **页面布局与文案校验场景** - 验证页面标题、字段标签

#### 弹窗常见测试场景
1. **弹窗表单验证场景** - 验证表单字段和验证规则
2. **弹窗交互场景** - 验证打开、关闭、取消等操作
3. **页面布局与文案校验场景** - 验证标题、提示文案

### 输出格式

```json
{
  "scenarios": [
    {
      "id": "scenario-1",
      "name": "测试场景名称",
      "description": "该场景的测试目标和范围",
      "priority": "high|medium|low",
      "relatedSections": ["1.1 需求章节标题", "1.1.1 子章节标题"],
      "estimatedTestPoints": 5
    }
  ]
}
```

### 关键要求

#### 1. relatedSections格式要求（极其重要！）

**格式规范**：
- 必须是"章节编号 章节标题"（章节编号和标题之间有一个空格）
- 章节编号必须与需求文档中的 ### 或 #### 标题完全一致

**正确示例**：
- "1.1 用户登录功能" ✅
- "1.1.1 页面布局与文案校验" ✅
- "1.2.3 操作按钮" ✅

**错误示例**（绝对禁止）：
- "1. 用户登录" ❌（缺少小数点后的编号）
- "登录功能" ❌（缺少章节编号）
- "1.1" ❌（缺少章节标题）
- "1.1  用户登录" ❌（两个空格）


#### 2. 章节完整性要求

**核心原则**：一个测试场景通常对应一个页面或模块的所有相关章节

**示例1 - 登录页面场景**（需求文档有 1.1、1.1.1、1.1.2、1.1.3、1.1.4）：
```json
{
  "relatedSections": [
    "1.1 用户登录页面",
    "1.1.1 页面布局与文案校验",
    "1.1.2 表单字段",
    "1.1.3 操作按钮",
    "1.1.4 业务规则"
  ]
}
```
说明：包含主章节 1.1 和所有子章节 1.1.1 到 1.1.4

**示例2 - 订单列表页面场景**（需求文档有 1.2、1.2.1、1.2.2、1.2.3）：
```json
{
  "relatedSections": [
    "1.2 订单列表页面",
    "1.2.1 查询条件",
    "1.2.2 列表展示字段",
    "1.2.3 操作按钮"
  ]
}
```
说明：包含主章节 1.2 和所有子章节 1.2.1 到 1.2.3

**常见错误**：
- ❌ 错误1：所有章节编号都是 1.1（必须使用不同的编号：1.1、1.1.1、1.1.2）
- ❌ 错误2：只关联父章节（遗漏了所有子章节）
- ❌ 错误3：空数组（必须至少包含一个章节）

#### 3. 排序要求（最高优先级！）

**强制排序规则**：
1. **第一个场景必须是主流程场景，优先级必须为 "high"**
   - 核心业务功能、主要操作流程
   - 例如：登录场景、订单创建场景、数据查询场景

2. **所有场景必须按优先级排序：high → medium → low**
   - 所有 high 优先级场景排在最前面
   - 然后是 medium 优先级场景
   - 最后是 low 优先级场景

**输出前检查清单**：
- [ ] 第一个场景是 high 优先级？
- [ ] 所有 high 在 medium 之前？
- [ ] 所有 medium 在 low 之前？

**正确示例**：
```json
[
  {"priority":"high"},
  {"priority":"high"},
  {"priority":"medium"},
  {"priority":"low"}
]
```

**错误示例**：
```json
[
  {"priority":"low"},
  {"priority":"high"}
]
```

#### 4. estimatedTestPoints说明

- 预估该场景需要多少个测试点
- 根据场景复杂度判断：
  - 简单场景：3-5个测试点
  - 中等场景：5-8个测试点
  - 复杂场景：8-12个测试点

### 重要提示

- 场景数量控制在3-6个，太多会导致规划过于分散
- 优先级判断：功能性 > 权限 > 布局文案
- 每个场景要有清晰的测试边界，避免重叠
- 场景名称应该具体明确，如"用户登录场景"、"订单查询场景"等

---

## 测试点生成提示词（阶段2）

**文件位置**: `server/services/functionalTestCaseAIService.ts` - `generateTestPointsForScenario`方法

**核心功能**:
- 为指定测试场景生成多个测试点
- 每个测试点包含：名称、描述、步骤、预期结果、风险等级
- 按主流程优先 + 风险等级排序

### 测试点设计原则

**测试点是什么？**
- 测试点是一个独立的、可执行的测试项
- 每个测试点包含：测试点名称、操作步骤、预期结果、风险等级
- 测试点应该原子化，一个测试点验证一个具体场景

**如何设计测试点？**
1. **正常流程**：验证标准操作路径
2. **异常流程**：验证错误处理和提示
3. **边界条件**：验证特殊值、空值、超长输入等
4. **权限控制**：验证不同角色的权限限制

**风险等级判断**：
- **high**：核心功能、资金相关、权限控制
- **medium**：常用功能、数据校验
- **low**：UI展示、文案校验

**数量建议**：
- 简单场景：3-5个测试点
- 复杂场景：6-10个测试点
- 根据场景复杂度决定，不要人为限制

### 主流程识别与排序（极其重要！）

**第一条测试点必须是主流程测试点（正常流程、正向场景）！**

#### 主流程测试点特征

1. **正常操作路径**：使用正确的数据、正确的操作顺序
2. **成功场景**：验证功能在理想条件下能够正常工作
3. **核心功能**：最基本、最常用的业务流程
4. **正向验证**：不是错误场景、不是边界条件、不是异常情况

#### 排序规则（必须严格遵守！）

1. **第一位：主流程测试点**（正常流程、正向场景、成功案例）
   - 例如："用户名和密码均正确，凭据正确" ✅（主流程）
   - 例如："正常提交订单" ✅（主流程）
   - 例如："有效数据查询" ✅（主流程）
   - ❌ 不是："用户名和密码均不为空，但凭据错误"（异常流程）
   - ❌ 不是："密码错误"（异常流程）
   - ❌ 不是："库存不足时提交"（异常流程）
   - 风险等级：通常为 high

2. **第二位：关键异常流程**（业务规则验证、重要异常处理）
   - 例如："用户名和密码均不为空，但凭据错误"
   - 例如："库存不足时提交订单"
   - 风险等级：high 或 medium

3. **后续：边界条件和次要异常**
   - 例如："用户名为空"、"密码为空"
   - 风险等级：medium 或 low

#### 识别主流程的关键词

- ✅ 正确、正常、有效、成功、合法、标准、凭据正确、密码正确、数据有效
- ❌ 错误、无效、失败、异常、非法、边界、为空、超长、凭据错误、密码错误


### 输出格式

```json
{
  "testPoints": [
    {
      "testPoint": "测试点名称",
      "description": "测试点描述（简要说明该测试点的目的和范围）",
      "coverageAreas": "覆盖范围（如：查询功能、数据校验、权限控制等）",
      "estimatedTestCases": 1,
      "steps": "1. 步骤1\n2. 步骤2\n3. 步骤3",
      "expectedResult": "预期结果描述",
      "riskLevel": "high|medium|low"
    }
  ]
}
```

### 字段说明

- **testPoint**: 测试点名称，简洁明确（必须与steps中的操作内容一致）
- **description**: 测试点描述，说明该测试点的测试目的和测试范围
- **coverageAreas**: 覆盖范围，说明该测试点覆盖的功能点
- **estimatedTestCases**: 预估该测试点会生成多少个测试用例
  - 简单测试点：1个
  - 中等测试点：2-3个
  - 复杂测试点：3-5个
- **steps**: 测试步骤，详细的操作步骤（必须与testPoint名称描述的场景一致）
- **expectedResult**: 预期结果，期望的测试结果（必须与testPoint名称和steps对应）
- **riskLevel**: 风险等级

### 数据一致性要求（极其重要！）

- 测试点名称、操作步骤、预期结果必须逻辑一致，不能相互矛盾
- 例如：测试点名为"用户名为空"，则steps中不能输入用户名
- 例如：测试点名为"密码为空"，则steps中不能输入密码

### 正确示例

```json
{
  "testPoints": [
    {
      "testPoint": "用户名和密码均正确，凭据正确",
      "riskLevel": "high"
    },
    {
      "testPoint": "用户名和密码均不为空，但凭据错误",
      "riskLevel": "high"
    },
    {
      "testPoint": "用户名为空",
      "riskLevel": "medium"
    },
    {
      "testPoint": "密码为空",
      "riskLevel": "medium"
    }
  ]
}
```

说明：
1. ✅ 主流程（正向场景）排第一
2. 异常流程（关键异常）排第二
3. 边界条件排后续

### 错误示例（绝对禁止）

```json
{
  "testPoints": [
    {
      "testPoint": "用户名和密码均不为空，但凭据错误",
      "riskLevel": "high"
    },
    {
      "testPoint": "用户名和密码均正确，凭据正确",
      "riskLevel": "high"
    }
  ]
}
```

说明：❌ 异常流程排第一（错误！），主流程排最后（错误！）

### 输出前检查清单

- [ ] 第一个测试点是正常流程（不是错误、不是为空、不是异常）？
- [ ] 第一个测试点的风险等级是 high？
- [ ] 测试点按照：正常流程 → 异常流程 → 边界条件 排序？

---

## 测试用例生成提示词（阶段3）

**文件位置**: `server/services/functionalTestCaseAIService.ts` - `generateTestCaseForTestPoint`方法

**核心功能**:
- 为单个测试点生成1个测试用例
- 包含：用例名称、测试步骤、预期结果、前置条件、测试数据
- 确保数据一致性（用例名称、测试数据、操作步骤必须完全一致）

### 最重要要求：数据一致性

**在生成每个测试用例时，必须确保**：
1. ✅ **用例名称** 与 **测试数据** 完全一致
2. ✅ **测试数据** 与 **操作步骤** 完全一致
3. ✅ **操作步骤** 必须真实执行用例名称描述的场景

**禁止出现的错误**：
- ❌ 用例名为"用户名为空"，但测试数据或步骤中有用户名的具体值
- ❌ 用例名为"正常登录"，但步骤中说"保持为空"
- ❌ 测试数据写了"用户名：admin"，但步骤中没有输入admin
- ❌ 不同用例的数据和步骤互相搞混

**生成策略：逐个用例独立思考**
- 先写用例名称 → 再写匹配的测试数据 → 最后写匹配的操作步骤
- 每个用例生成完后，必须回头检查三者是否一致
- 不要把不同用例的数据混在一起！

### 正确示例

**示例1：用户名不为空，密码为空 - BOUNDARY**
```json
{
  "name": "用户名不为空，密码为空",
  "testData": "用户名：admin\n密码：（空）",
  "steps": "1. 【操作】打开登录页面\n   【预期】页面正常加载\n2. 【操作】在用户名输入框中输入'admin'\n   【预期】输入框正常接收输入\n3. 【操作】密码输入框保持为空\n   【预期】密码输入框保持空白状态\n4. 【操作】点击登录按钮\n   【预期】显示错误提示：密码不能为空"
}
```
说明：✅ 用例名说"密码为空"，testData标记"密码：（空）"，steps说"保持为空" - 三者完全一致！

**示例2：用户名为空，密码不为空 - BOUNDARY**
```json
{
  "name": "用户名为空，密码不为空",
  "testData": "用户名：（空）\n密码：123456",
  "steps": "1. 【操作】打开登录页面\n   【预期】页面正常加载\n2. 【操作】用户名输入框保持为空\n   【预期】用户名输入框保持空白状态\n3. 【操作】在密码输入框中输入'123456'\n   【预期】输入框正常接收输入\n4. 【操作】点击登录按钮\n   【预期】显示错误提示：用户名不能为空"
}
```
说明：✅ 用例名说"用户名为空"，testData标记"用户名：（空）"，steps说"保持为空" - 三者完全一致！

### 错误示例（绝对禁止）

```json
{
  "name": "用户名为空，密码不为空",
  "testData": "用户名：（空）\n密码：123456",
  "steps": "1. 打开登录页面\n2. 在用户名输入框中输入'admin'\n3. 在密码输入框中输入'123456'"
}
```
说明：❌❌❌ 用例名和testData说"用户名为空"，但steps却在输入admin，完全矛盾！这种用例会被系统自动过滤掉！


### 检查清单（生成前必须逐项检查）

- [ ] 用例名称描述的场景与测试数据一致？
- [ ] 测试数据与操作步骤中使用的数据一致？
- [ ] 操作步骤是否真的在执行用例名称描述的场景？
- [ ] 预期结果是否符合用例名称描述的场景？
- [ ] 如果用例名称说"为空"，则测试数据必须标记为"（空）"，步骤中不能输入该字段
- [ ] 如果用例名称说"不为空"，则测试数据必须有具体值，步骤中必须输入该字段
- [ ] 步骤中使用的所有具体值（如 'admin'、'123456'）必须在测试数据中列出

### 输出格式

```json
{
  "testCases": [
    {
      "name": "测试用例名称",
      "description": "测试用例描述",
      "testScenario": "测试场景名称",
      "testPoint": "测试点名称",
      "system": "系统名",
      "module": "模块名",
      "sectionId": "1.1",
      "sectionName": "章节名称",
      "priority": "high|medium|low",
      "caseType": "SMOKE|FULL|BOUNDARY|ABNORMAL",
      "tags": ["标签1", "标签2"],
      "preconditions": "前置条件",
      "testData": "用户名：admin\n密码：123456\n邮箱：test@example.com",
      "steps": "1. 【操作】具体操作步骤1\n   【预期】该步骤的预期结果\n2. 【操作】具体操作步骤2\n   【预期】该步骤的预期结果\n3. 【操作】具体操作步骤3\n   【预期】该步骤的预期结果",
      "assertions": "1. 步骤1的预期结果\n2. 步骤2的预期结果\n3. 步骤3的预期结果（保持与steps中的预期一致）"
    }
  ]
}
```

### 步骤格式要求

**每个测试步骤必须包含两部分**：
1. **【操作】** - 具体的操作动作
2. **【预期】** - 该操作的预期结果

**格式示例**：
```
1. 【操作】打开用户登录页面
   【预期】页面正常加载，显示用户名和密码输入框
2. 【操作】输入正确的用户名"admin"和密码"123456"
   【预期】输入框正常接收输入，密码显示为掩码
3. 【操作】点击"登录"按钮
   【预期】系统验证通过，跳转到首页，显示欢迎信息
```

**assertions字段说明**：
- assertions字段应该汇总所有步骤的预期结果
- 数量必须与steps中的步骤数量一致
- 内容应该与steps中的【预期】部分保持一致
- 每条预期结果独立成行，便于测试人员验证

### 用例类型判断规则（caseType）

**强制要求：只有当测试点本身是正常流程时，才生成 SMOKE 类型的冒烟用例！**

- **SMOKE（冒烟用例）**【优先级必须为 high】：
  - 核心功能验证、基本正向流程、高优先级场景、主要业务路径
  - 示例：用户登录的正常流程、订单创建的基本流程、数据查询的主要功能
  - 判断标准：如果该功能不通过，整个模块基本不可用
  - 🔥 **SMOKE 用例的 priority 字段必须设置为 "high"**
  
- **FULL（全量用例）**：详细功能验证、边界条件测试、异常情况处理
- **BOUNDARY（边界用例）**：边界条件测试、各种输入组合
- **ABNORMAL（异常用例）**：异常情况处理、各种输入组合


### 用例数量要求

**核心原则**：每个测试点生成1个用例，专注于当前测试点描述的场景

**重要规则**：
- **只生成与当前测试点直接相关的用例**
- **不要生成与测试点无关的正常流程用例**
- 例如：
  - 测试点是"用户名为空，密码正确" → 只生成"用户名为空，密码正确"用例，不要生成"正常登录"
  - 测试点是"密码错误" → 只生成"密码错误"用例，不要生成"正常登录"
  - 测试点是"用户名和密码均正确" → 生成"正常登录"用例

**特殊情况**：
- 如果测试点本身包含多种数据组合（如"用户名超长字符输入"可能需要测试不同长度），可以生成1-2个用例

### 主流程用例识别

**只有当测试点本身描述的是正常流程时，才生成 SMOKE 类型的主流程用例！**

**主流程测试点特征**：
- 测试点名称包含：正常、正确、有效、成功、合法、标准、均正确、均有效
- 测试点描述的是理想条件下的操作路径
- 例如："用户名和密码均正确，凭据正确"、"正常提交订单"

**非主流程测试点特征**：
- 测试点名称包含：错误、无效、失败、异常、为空、超长、边界、不正确
- 测试点描述的是边界条件或异常情况
- 例如："用户名为空"、"密码错误"、"用户名超长字符输入"

**用例类型判断规则**：
- 如果测试点是主流程 → 生成 SMOKE 类型用例，priority 为 high
- 如果测试点是边界条件 → 生成 BOUNDARY 类型用例，priority 为 medium
- 如果测试点是异常情况 → 生成 ABNORMAL 类型用例，priority 为 medium

### 测试数据要求

**testData 字段格式要求**：
- **必须换行显示**：每个字段占一行，格式为"字段名：值\n"
- 示例："用户名：admin\n密码：123456\n邮箱：test@example.com"
- ⚠️ 如果字段为空，明确标记："字段名：（空）"
- 如果步骤中使用了具体的测试数据，必须在 testData 字段中列出
- 确保与用例名称、步骤内容完全一致

### 生成前自查清单（每个用例生成前必须逐项检查）

**为了避免数据混乱，请在生成每个用例时，严格按照以下步骤自查**：

1. **读取用例名称** → 确定该用例要测试什么场景
2. **编写测试数据** → 根据用例名称填写 testData 字段
   - 例如：名称是"用户名为空"，则 testData 必须写 "用户名：（空）"
   - 例如：名称是"正常登录"，则 testData 必须写具体的用户名和密码值
3. **编写操作步骤** → steps 中的操作必须与 testData 完全一致
   - 如果 testData 中某字段为（空），则 steps 中该字段必须"保持为空"
   - 如果 testData 中某字段有具体值，则 steps 中必须"输入该具体值"
4. **交叉验证** → 用例名称、testData、steps 三者必须完全一致，不能有任何矛盾

**特别警告**：
- ⛔ 绝对不允许出现：用例名为"用户名为空"，但 testData 或 steps 中有用户名的具体值
- ⛔ 绝对不允许出现：用例名为"用户名不为空"，但 testData 或 steps 中没有用户名
- ⛔ 如果 testData 写了 "用户名：admin"，则 steps 必须写 "输入'admin'"
- ⛔ 如果 testData 写了 "密码：（空）"，则 steps 必须写 "密码保持为空"
- ⛔ 不要把不同用例的数据和步骤搞混！每个用例独立检查！

---

## 项目配置集成

**功能**：将项目配置（服务器地址、账号密码）注入到测试用例生成中

**配置来源**：`TestConfigService.getProjectDefaultConfig(projectId)`

**配置内容**：
```typescript
{
  serverUrl: "https://172.19.5.47:443",
  loginUrl: "https://172.19.5.47:443/login",
  accounts: {
    admin: { username: "sysadmin", password: "******" },
    security: { username: "security", password: "******" },
    auditor: { username: "auditor", password: "******" },
    default: { username: "testuser", password: "******" }
  }
}
```

**使用方式**：
- 在生成测试步骤时，自动使用项目配置的地址和账号
- 例如：步骤1自动填充为"访问登录页面：https://172.19.5.47:443/login"
- 例如：步骤2自动填充为"输入账号：sysadmin"

**提示词注入**：
```
## 项目配置信息（用于生成测试步骤）

**服务器访问地址**: https://172.19.5.47:443
**登录页面地址**: https://172.19.5.47:443/login

**可用账号**:
- 管理员账号: sysadmin / ******
- 安全审计账号: security / ******
- 审核员账号: auditor / ******
- 默认测试账号: testuser / ******

**生成测试步骤时的要求**:
1. 如果步骤涉及访问页面，使用上述服务器地址
2. 如果步骤涉及登录，使用上述账号信息
3. 根据测试场景选择合适的账号（如权限测试使用不同角色账号）
```

---

## RAG知识库增强

**功能**：通过向量搜索，将团队测试经验融入AI生成

**知识库类型**：
1. **测试规则**：业务规则、验证规则、拦截规则
2. **测试模式**：常见测试场景、测试策略
3. **历史问题**：已知缺陷、易错点
4. **风险场景**：高风险操作、边界条件

**使用流程**：
1. 根据测试点内容查询知识库（topK=3, scoreThreshold=0.5）
2. 将相关知识作为上下文注入到提示词中
3. AI生成时参考知识库内容

**知识库查询示例**：
```typescript
const queryText = `${testPoint.testPoint}\n${testPoint.steps}\n${scenarioName}`;
const knowledgeResults = await knowledgeBase.searchByCategory({
  query: queryText,
  topK: 3,
  scoreThreshold: 0.5
});
```

**提示词注入示例**：
```
## 📚 相关测试知识（来自团队知识库）

### 测试规则
1. 订单金额必须大于0，否则提示"订单金额不能为0或负数"
2. 审核通过时，需要校验库存是否能扣减成功

### 历史问题
1. 曾出现过：用户名输入特殊字符导致SQL注入漏洞
2. 曾出现过：密码长度超过50字符导致系统崩溃

### 风险场景
1. 高风险：涉及资金操作的功能，必须有二次确认
2. 高风险：权限控制不严格，可能导致越权访问

**请在生成测试用例时，参考上述知识库内容，确保覆盖已知风险点。**
```

---

## 修改页面模式

**触发条件**：需求文档包含【变更摘要】章节

**识别方式**：
```typescript
const hasChangeSummary = /##\s*变更摘要/.test(requirementDoc);
const isModifyMode = hasChangeSummary;
```

### 变更摘要格式

```markdown
## 变更摘要

### 🆕 新增功能
- 列表新增【订单来源】字段
- 筛选条件新增【订单来源】下拉框
- 新增【渠道集采订单编号】字段

### ✏️ 修改功能
- 含税采购价计算逻辑调整：根据订单类型采用不同方式
- 订单状态流转规则优化

### ❌ 删除功能
- 移除【旧字段名】字段

### ➖ 原有功能（未变更）
- 订单列表基本展示
- 供应商筛选
- 导出功能
```

### 特殊处理规则

**阶段1：测试场景拆分**
1. 从需求文档中找到【变更摘要】章节
2. 只针对 🆕 新增功能 和 ✏️ 修改功能 生成测试场景
3. 完全忽略 ➖ 原有功能（未变更）部分
4. 测试场景命名要明确标注是"新增"还是"修改"

**示例**：
```json
{
  "scenarios": [
    {
      "name": "订单来源字段测试（新增）",
      "description": "验证新增的订单来源字段显示和筛选功能"
    },
    {
      "name": "含税采购价计算逻辑测试（修改）",
      "description": "验证修改后的含税采购价计算逻辑"
    }
  ]
}
```

**阶段2：测试点生成**
- 当前测试场景应该是针对变更功能的
- 生成的测试点应该聚焦于这些变更点
- 如果是新增功能，要完整测试
- 如果是修改功能，要重点测试变更的逻辑和影响

**阶段3：测试用例生成**
- 生成的测试用例应该聚焦于变更的功能点
- 新增功能：完整测试新增的字段、按钮、逻辑
- 修改功能：重点测试修改的逻辑、变更的计算方式、调整的规则
- 回归测试：验证修改是否影响相关联的功能

### 测试重点建议

在需求文档末尾增加【测试重点】章节：

```markdown
## 测试重点

### 新增功能测试重点
1. 订单来源字段显示是否正确
2. 订单来源筛选功能是否正常
3. 渠道集采订单编号是否正确显示

### 修改功能测试重点
1. 含税采购价计算逻辑是否按新规则执行
2. 不同订单类型的计算方式是否正确
3. 修改是否影响原有订单数据

### 回归测试重点
1. 原有的订单列表展示功能是否正常
2. 原有的导出功能是否受影响
3. 原有的供应商筛选功能是否正常
```

---

## 提示词调用流程

### 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│  用户上传Axure原型 / 输入文本需求                              │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段0：需求文档生成                                           │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ • Web端: web-requirement.prompt.ts                  │    │
│  │ • 移动端: mobile-requirement.prompt.ts              │    │
│  │ • 文本: text-requirement.prompt.ts                  │    │
│  │ • 通用指令: common-instructions.ts                  │    │
│  └─────────────────────────────────────────────────────┘    │
│  输出：结构化需求文档（Markdown格式）                          │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段1：测试场景拆分                                           │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 方法: analyzeTestScenarios()                        │    │
│  │ 输入: 需求文档                                       │    │
│  │ 处理:                                               │    │
│  │   1. 识别页面类型（列表页/表单页/详情页/弹窗）        │    │
│  │   2. 拆分测试场景（3-6个）                           │    │
│  │   3. 关联需求章节                                    │    │
│  │   4. 预估测试点数量                                  │    │
│  │   5. 按优先级排序（high → medium → low）            │    │
│  └─────────────────────────────────────────────────────┘    │
│  输出：3-6个测试场景（JSON格式）                              │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段2：测试点生成                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 方法: generateTestPointsForScenario()               │    │
│  │ 输入: 单个测试场景 + 需求文档                        │    │
│  │ 处理:                                               │    │
│  │   1. 提取相关章节内容                                │    │
│  │   2. 设计测试点（正常/异常/边界/权限）               │    │
│  │   3. 判断风险等级（high/medium/low）                │    │
│  │   4. 主流程优先排序                                  │    │
│  └─────────────────────────────────────────────────────┘    │
│  输出：每个场景3-10个测试点（JSON格式）                       │
│  循环：为每个场景生成测试点                                   │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段3：测试用例生成                                           │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 方法: generateTestCaseForTestPoint()                │    │
│  │ 输入: 单个测试点 + 场景信息 + 需求文档 + 项目配置    │    │
│  │ 处理:                                               │    │
│  │   1. 获取项目配置（服务器地址、账号密码）            │    │
│  │   2. 查询RAG知识库（相关测试经验）                   │    │
│  │   3. 生成测试用例（1个）                             │    │
│  │   4. 确保数据一致性（名称/数据/步骤）                │    │
│  │   5. 判断用例类型（SMOKE/BOUNDARY/ABNORMAL）        │    │
│  └─────────────────────────────────────────────────────┘    │
│  输出：每个测试点1个测试用例（JSON格式）                      │
│  循环：为每个测试点生成测试用例                               │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段4：数据一致性验证                                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 检查项:                                             │    │
│  │   1. 用例名称 vs 测试数据                            │    │
│  │   2. 测试数据 vs 操作步骤                            │    │
│  │   3. 操作步骤 vs 预期结果                            │    │
│  │ 处理:                                               │    │
│  │   • 有效用例：保留                                   │    │
│  │   • 无效用例：过滤并标记原因                         │    │
│  └─────────────────────────────────────────────────────┘    │
│  输出：有效测试用例列表 + 过滤用例列表                        │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  最终输出：完整的测试用例集                                    │
│  • 需求文档（Markdown）                                       │
│  • 测试场景列表（3-6个）                                      │
│  • 测试点列表（每个场景3-10个）                               │
│  • 测试用例列表（每个测试点1个）                              │
│  • 数据一致性验证报告                                         │
└─────────────────────────────────────────────────────────────┘
```

### 关键节点说明

**节点1：需求文档生成**
- 时间：10-30秒
- AI调用：1次
- Token消耗：约2000-4000 tokens

**节点2：测试场景拆分**
- 时间：5-10秒
- AI调用：1次
- Token消耗：约1000-2000 tokens
- 输出：3-6个场景

**节点3：测试点生成**
- 时间：每个场景5-10秒
- AI调用：场景数量次（3-6次）
- Token消耗：每次约1000-2000 tokens
- 输出：每个场景3-10个测试点

**节点4：测试用例生成**
- 时间：每个测试点5-10秒
- AI调用：测试点数量次（15-60次）
- Token消耗：每次约2000-4000 tokens
- 输出：每个测试点1个测试用例

**总计**：
- 总时间：约5-15分钟（取决于复杂度）
- 总AI调用：约20-70次
- 总Token消耗：约30,000-200,000 tokens
- 总输出：15-60个测试用例

---

## 附录：关键代码位置

### 提示词文件

| 文件路径 | 功能描述 |
|---------|---------|
| `server/prompts/web-requirement.prompt.ts` | Web端需求文档生成提示词 |
| `server/prompts/mobile-requirement.prompt.ts` | 移动端需求文档生成提示词 |
| `server/prompts/text-requirement.prompt.ts` | 文本文档需求提取提示词 |
| `server/prompts/common-instructions.ts` | 通用指令和格式规范 |

### 核心服务文件

| 文件路径 | 功能描述 |
|---------|---------|
| `server/services/functionalTestCaseAIService.ts` | AI测试用例生成核心服务 |
| `server/services/aiParser.ts` | Axure HTML解析服务 |
| `server/services/testConfigService.ts` | 项目配置服务 |
| `server/services/testCaseKnowledgeBase.ts` | RAG知识库服务 |

### 关键方法

| 方法名 | 所在文件 | 功能描述 |
|-------|---------|---------|
| `analyzeTestScenarios()` | `functionalTestCaseAIService.ts` | 阶段1：测试场景拆分 |
| `generateTestPointsForScenario()` | `functionalTestCaseAIService.ts` | 阶段2：测试点生成 |
| `generateTestCaseForTestPoint()` | `functionalTestCaseAIService.ts` | 阶段3：测试用例生成 |
| `getWebRequirementPrompt()` | `web-requirement.prompt.ts` | 获取Web端需求文档提示词 |
| `getMobileRequirementPrompt()` | `mobile-requirement.prompt.ts` | 获取移动端需求文档提示词 |
| `getTextRequirementPrompt()` | `text-requirement.prompt.ts` | 获取文本需求提取提示词 |
| `getUserPrompt()` | `common-instructions.ts` | 获取用户提示词（通用部分） |
| `getBusinessRulesSystemPrompt()` | `common-instructions.ts` | 获取补充业务规则提示词 |
| `getCommonSystemInstructions()` | `common-instructions.ts` | 获取通用系统指令 |

### API路由

| 路由路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/v1/functional-test-cases/analyze-scenarios` | POST | 阶段1：测试场景拆分 |
| `/api/v1/functional-test-cases/generate-points-for-scenario` | POST | 阶段2：测试点生成 |
| `/api/v1/functional-test-cases/generate-test-case-for-point` | POST | 阶段3：测试用例生成 |

### 数据类型定义

| 类型名 | 所在文件 | 描述 |
|-------|---------|------|
| `TestScenario` | `functionalTestCaseAIService.ts` | 测试场景数据结构 |
| `TestPoint` | `functionalTestCaseAIService.ts` | 测试点数据结构 |
| `TestCase` | `functionalTestCaseAIService.ts` | 测试用例数据结构 |
| `TestCaseGenerationResult` | `functionalTestCaseAIService.ts` | 测试用例生成结果（包含过滤信息） |
| `ProjectConfigForAI` | `functionalTestCaseAIService.ts` | 项目配置信息（用于AI生成） |

---

## 总结

本文档详细整理了Sakura AI系统中所有AI生成相关的提示词，涵盖了从需求文档生成到测试用例生成的完整流程。

### 核心特点

1. **多阶段生成**：需求文档 → 测试场景 → 测试点 → 测试用例
2. **智能识别**：自动识别页面类型、弹窗、多状态画面
3. **数据一致性**：严格验证用例名称、测试数据、操作步骤的一致性
4. **主流程优先**：确保正常流程测试点和冒烟用例排在最前面
5. **RAG增强**：集成团队测试经验，提升生成质量
6. **项目配置集成**：自动使用项目配置的服务器地址和账号信息

### 质量保证

- **格式规范**：严格的Markdown格式和JSON输出格式
- **质量检查**：多层次的质量检查清单
- **错误修正**：10种常见错误及修正方法
- **数据验证**：自动过滤数据不一致的测试用例

### 适用场景

- Web端Axure原型测试用例生成
- 移动端Axure原型测试用例生成（支持多状态画面）
- PDF/DOCX/Markdown/TXT文本需求提取
- 修改页面的增量测试用例生成

### 维护建议

1. 定期更新提示词，根据实际使用效果优化
2. 收集AI生成的错误案例，补充到常见错误列表
3. 持续完善RAG知识库，提升生成质量
4. 根据用户反馈，调整测试场景拆分策略

---

**文档结束**

如有疑问或建议，请联系Sakura AI开发团队。
